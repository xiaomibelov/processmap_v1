# RUNTIME_NAVIGATION — stage1/test-e2e-smoke-20260613T074137Z

> To be filled by Agent 2 (Worker) during execution.

## Target Runtime
- Frontend URL: `http://127.0.0.1:5177` (override with `E2E_APP_BASE_URL`)
- Backend URL: `http://127.0.0.1:8011` (override with `E2E_API_BASE_URL`)
- Browser: `webkit` (override with `E2E_BROWSER`)

## Navigation Steps
1. (Worker to fill) Exact entry URL and any login/auth steps
2. (Worker to fill) How the workspace dashboard under test is reached
3. (Worker to fill) Any prerequisite data or fixtures

## Environment Notes
- This contour validates the workspace-dashboard e2e smoke path only
