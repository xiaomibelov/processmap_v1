# VERSION_UPDATE_LEDGER_PROOF

Version ledger updated in:

```text
frontend/src/config/appVersion.js
```

Change:

- `currentVersion`: `v1.0.126` -> `v1.0.127`
- New changelog entry:
  - `Реестр действий с продуктом переведен на единую белую рабочую поверхность с компактными фильтрами, метриками и таблицей.`

Build validation:

- `npm run build` completed successfully in `/opt/processmap-product-actions-single-surface-part1/frontend`.
- No package install was performed.
