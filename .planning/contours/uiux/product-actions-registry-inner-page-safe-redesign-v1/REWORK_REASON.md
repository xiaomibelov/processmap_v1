# REWORK_REASON — Переоткрытие контура для runtime-proof доработки

**Контур:** `uiux/product-actions-registry-inner-page-safe-redesign-v1`  
**Новый Run ID:** `20260517T112506Z-72991`  
**Предыдущий Run ID:** `20260517T101528Z-68933`  
**Дата:** `2026-05-17`  
**Статус:** `SOURCE_TEST_PASS_RUNTIME_VISUAL_REVIEW_REQUIRED`

---

## 1. Почему контур переоткрыт

Предыдущий `REVIEW_PASS` (создан в рамках run `20260517T101528Z-68933`) **аннулируется** и считается историческим артефактом, но **не финальным вердиктом**.

Причины:

### 1.1. Runtime mismatch — критический факт
- **Исходный код / рабочая директория:** содержит версию `v1.0.135` (редизайн реестра выполнен, компоненты выделены, тесты проходят).
- **Runtime на `clearvestnic.ru:5180`:** в момент предыдущего ревью отдавал версию `v1.0.134`.
- **Следствие:** новый UI реестра **не был визуально проверен в браузере**. Ревью ограничилось код-анализом и тестами.

### 1.2. UI/UX работа требует визуального подтверждения
- Редизайн страницы «Реестр действий» — это **визуальная задача**.
- Source/test proof недостаточно: нужно увидеть метрики, фильтры, таблицу, пагинацию, баннер, поведение close/back в реальном браузере.
- Предыдущий `REVIEW_PASS` не содержит runtime visual evidence.

### 1.3. Reviewer report сам признал ограничение
В `REVIEW_REPORT.md` (run `20260517T101528Z-68933`) явно указано:

> **Finding 2 — Runtime version mismatch:**  
> Сервер `clearvestnic.ru:5180` отдаёт `v1.0.134`, тогда как код в рабочей директории — `v1.0.135`. Визуальная верификация нового UI реестра в браузере **не проводилась** из-за отсутствия деплоя.

Этот finding был отмечен, но не привёл к `REWORK_REQUEST`. Контур закрыт с `REVIEW_PASS`, что недопустимо при отсутствии визуального подтверждения.

---

## 2. Что сохраняется из предыдущего run

| Артефакт | Статус |
|----------|--------|
| `PLAN.md` | Сохраняется, не изменяется |
| `EXEC_REPORT.md` (Agent 2 + 3) | Сохраняется как история реализации |
| `REVIEW_REPORT.md` | Сохраняется как история ревью |
| `REVIEW_PASS` | **Сохраняется** как исторический артефакт, но **помечен superseded** |
| `STATE.json` | Архивирован в контексте; новый — `REWORK_STATE.json` |

---

## 3. Что изменяется в rework

- **Новый статус:** `SOURCE_TEST_PASS_RUNTIME_VISUAL_REVIEW_REQUIRED`.
- **Новый workflow:** 4-agent (Planner → Worker 2 + Worker 3 → Reviewer).
- **Новая цель:** обеспечить деплой `v1.0.135` на `5180` и провести **визуальное runtime-ревью**.
- **Agent 2:** фокус на runtime delivery (build, deploy proof, visual evidence capture).
- **Agent 3:** фокус на branch hygiene и scope classification (независимо от Agent 2).
- **Agent 4:** финальное runtime visual review **только после** подтверждения версии на `5180`.

---

## 4. Условия для финального REVIEW_PASS

Финальный `REVIEW_PASS` может быть выдан **только если**:

1. `clearvestnic.ru:5180` отдаёт `v1.0.135` (или текущую каноническую версию).
2. `build-info.json` и footer соответствуют текущему HEAD/build.
3. Имена JS/CSS-ассетов в `dist` совпадают с сервированными.
4. Страница «Реестр действий» визуально открыта и проверена в браузере.
5. Shell/header/global layout ProcessMap **не изменены**.
6. Analytics Hub работает, реестр вложен под ним.
7. Нет console errors от реестра.
8. Branch hygiene документирована (Analytics Hub + Registry redesign scope).
9. Нет out-of-scope backend/schema/BPMN/RAG изменений.

---

*Agent 1 / Planner*  
*Контур переоткрыт: 2026-05-17*
