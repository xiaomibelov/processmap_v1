# Agent 4 / Reviewer

Use this prompt as the executable reviewer prompt for:
`uiux/product-actions-registry-inner-page-safe-redesign-v1`

Read and follow:
`REVIEWER_REWORK_PROMPT.md`

Final validation may start only when both markers exist:

- `WORKER_2_DONE`
- `WORKER_3_DONE`

Current review status before validation:
`CHANGES_REQUESTED`

Do not write `REVIEW_PASS` unless the runtime UX paths and hygiene classification all pass.
