# Agent 2 / Executor Part 1

Use this prompt as the executable Worker 2 prompt for:
`uiux/product-actions-registry-inner-page-safe-redesign-v1`

Read and follow:
`WORKER_2_REWORK_PROMPT.md`

This part owns UI implementation only:

- empty workspace scope rendering;
- table shell/headers or deliberate empty-state visibility;
- AI controls in primary filters/actions area;
- AI controls removed from secondary source/session section;
- compact CSV/XLSX/export utility actions;
- clear `Вернуться` navigation action;
- version row update if product code changes.

Completion marker:
`WORKER_2_DONE`

Blocked marker:
`EXEC_PART_1_BLOCKED.md`
