# Plan

1. Reproduce the missing JSON content type at the endpoint-check API boundary.
2. Pass a plain object to the shared API client and keep serialization centralized.
3. Run targeted tests and the production frontend build.

