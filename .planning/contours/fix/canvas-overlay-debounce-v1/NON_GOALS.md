# Не-цели (строго запрещено)

**Контур**: `fix/canvas-overlay-debounce-v1`  
**Запуск**: `20260528T190318Z-4670`

---

## Запрещённые действия

1. **Viewport culling** — НЕ реализовывать повторно. Было откачено в `fix/canvas-viewport-culling-v1` из-за регрессии: фигуры исчезали при выходе из viewport, скруббер сломался.
2. **Удаление фигур из DOM** — НЕ удалять shape-элементы из DOM ни при каких условиях.
3. **Модификация ядра bpmn-js** — НЕ изменять файлы внутри `node_modules` или форк bpmn-js.
4. **Изменение backend** — НЕ трогать `backend/app/`, API endpoints, БД.
5. **Скруббер / minimap** — НЕ изменять логику scrubber или minimap.
6. **Удаление функциональности оверлеев** — badges, labels, indicators должны показывать корректные данные.
7. **Selection highlight** — НЕ debounce selection highlight (должен оставаться отзывчивым).
8. **Zoom / click / drag** — НЕ изменять поведение zoom, click, drag фигур.

## Почему это важно

- Culling и DOM removal уже доказали свою рискованность.
- Ядро bpmn-js — внешняя зависимость; изменения непереносимы.
- Backend не связан с клиентским рендерингом оверлеев.
- Скруббер — критический UI-элемент; трогать только при явной задаче.

## Worker обязан

- Проверить, что diff НЕ содержит код, связанный с culling или DOM removal.
- Если debounce-логика случайно затрагивает scrubber/minimap — откатить.
