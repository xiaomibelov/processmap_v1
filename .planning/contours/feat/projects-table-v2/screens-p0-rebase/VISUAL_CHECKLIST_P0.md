# P0-rebase — визуальный чек-лист (2026-08-16)

Прогон: dev-контур ветки на сервере (backend 127.0.0.1:8012 + vite 127.0.0.1:5299, БД processmap),
Playwright headless, скрипт-замер + проверка глазами. Скриншоты: `table_v2_{1440,1100,880,640}.png`.

## Программные маркеры (ALL_CHECKS_PASS)

- [x] Колонки «Название / Тип / Состав / Ответственный / Статус / Обновлено» присутствуют на всех 4 ширинах
- [x] Колонка «DoD» отсутствует на всех 4 ширинах (0 совпадений th /DoD/i)
- [x] Прогресс-бары «Состав» рендерятся (978 строк с барами в выборке)
- [x] Крошки #730 (`explorer-breadcrumbs`) на месте на всех ширинах
- [x] Колонка «Название»: 350px @1440, 260px @1100/880/640 — не схлопывается (регрессия «30px» не воспроизводится)
- [x] Временный горизонтальный скролл включается при <1044px (scrollX=true на 1100/880/640, false на 1440)
- [x] «Обновлено»: относительное время рендерится полностью (client==scroll, без обрезки), деталь «Папка/Проект «…»» — truncate + тултип (по дизайну)

## Глазами

- [x] 1440: сетка колонок ровная; «Состав» у разделов = «N проектов · бар · done/trackable»; у проектов = бар + счётчик; статус-пилюли (старый select, редизайн — P3) на месте; кнопка «Открыть проект» в колонке действий не перекрывает «Обновлено»
- [x] 1100/880: таблица уходит в h-scroll целиком, «Название» держит 260px, шапка колонок читается
- [x] 640: то же, двухстрочные карточки — за рамкой P0 (P4), h-scroll приемлем как временная мера
- [x] Тёмная тема: контраст текстов и пилюль читаемый

## Ограничения приёмки

- Аватары: в dev-БД нет назначенных ответственных — во всех строках «Не назначен» (приглушённо, корректно).
  Ветка аватара покрыта unit-тестами (`explorerTableFormat.test.mjs`, initials/color-hash), визуально не проверена.
- Данные: витрина dev-БД (`V4 shots project …`), реальные данные наружу не выставлялись (dev только 127.0.0.1).
