# REVIEWER_PROMPT — Analytics UI/UX + Additional Fields + Excel Export

**For:** Agent 3 / Reviewer  
**Contour:** `premium-urgent-task-analytics-ui-ux-addi-mqdketwb`  
**Working directory:** `/opt/processmap-test`

---

## 1. Review scope

Review the implementation produced by Agent 2 / Worker for:
1. Additional fields/columns in Session, Project, Workspace analytics dashboards
2. Excel export functionality
3. UI/UX polish (table, filters, responsive, skeleton)
4. Tests
5. Build, sync, deploy, runtime proof

---

## 2. Acceptance criteria

### A. Additional fields

- [ ] `SessionAnalyticsDashboard.jsx` shows a registry/table with columns:
  - `Duration`
  - `Status`
  - `Created By`
- [ ] `ProjectAnalyticsDashboard.jsx` table includes:
  - `Total Sessions`
  - `Last Activity`
  - `Owner`
- [ ] `WorkspaceAnalyticsDashboard.jsx` shows fields:
  - `Member Count`
  - `Project Count`
  - `Storage Used`
- [ ] All new columns/fields are sortable
- [ ] All new columns/fields are filterable
- [ ] Missing backend fields are mocked with `// TODO(backend):` comments and fallback `—`

### B. Excel export

- [ ] Export button exists on each dashboard, top-right near filters
- [ ] Button has `data-testid="analytics-export-excel"`
- [ ] Clicking export downloads a `.xlsx` file
- [ ] Filename format: `processmap-analytics-{surface}-{YYYY-MM-DD}.xlsx`
- [ ] Workbook contains:
  - Sheet 1: Metadata (surface, generatedAt, applied filters)
  - Sheet 2: Data (all visible columns)
- [ ] Export reflects current filtered/sorted view

### C. UI/UX polish

- [ ] Table headers are sticky
- [ ] Rows have zebra striping
- [ ] Rows have hover highlight
- [ ] Inline filter chips show active filters
- [ ] Clear-all button resets filters
- [ ] Saved filter presets work via localStorage
- [ ] Table is responsive: horizontal scroll or card collapse below 768px
- [ ] Empty state for "no matching filters" shows applied filters + Clear filters CTA
- [ ] Skeleton rows match table column count

### D. Feature flag

- [ ] `USE_ANALYTICS_FIELDS_EXPORT` exists in `dashboardConfig.js`
- [ ] New functionality is guarded by the flag
- [ ] When flag is `false`, dashboards render as before (backward compatible)

### E. Code quality

- [ ] No modifications to `TopBarContainer`, `Mz`, `TG`, `_m`, `Td`, `xm`, `vm`, Auth guard
- [ ] No overlay navigation breakage
- [ ] No new backend endpoints unless justified by >10k rows
- [ ] No secrets or credentials in code
- [ ] Minimal, bounded changes

### F. Tests

- [ ] At least 3 new test cases added to `AnalyticsDashboards.test.mjs`
- [ ] Tests cover: table render, filter apply, export button
- [ ] `npm test` passes

### G. Build + sync

- [ ] `npm ci && npm run build` passes in `/opt/processmap-test/frontend`
- [ ] `/root/processmap_v1/frontend` is synced with changes
- [ ] `package.json` and `package-lock.json` both include `xlsx`

### H. Deploy + runtime proof

- [ ] Deployed to `clearvestnic.ru:5177`
- [ ] `curl` health check succeeds
- [ ] Screenshots show new fields on Session/Project/Workspace dashboards
- [ ] Screenshot or video shows Excel export button + downloaded file
- [ ] `RUNTIME_PROOF_CHECKLIST.md` is filled
- [ ] Git tag `pre-fields-export` exists

---

## 3. Review process

1. Read `PLAN.md`, `WORKER_PROMPT.md`, `EXEC_REPORT.md`, `RUNTIME_PROOF_CHECKLIST.md`
2. Check git state:
   ```bash
   cd /opt/processmap-test
   git branch --show-current
   git status -sb
   git diff --stat origin/main
   ```
3. Run build:
   ```bash
   cd /opt/processmap-test/frontend
   npm ci && npm run build
   ```
4. Run tests:
   ```bash
   npm test
   ```
5. Inspect key files for criteria A–E
6. Verify sync to `/root/processmap_v1/frontend`
7. Verify runtime on `clearvestnic.ru:5177`
8. Produce `REVIEW_REPORT.md` with:
   - Verdict (PASS / BLOCKED / NEEDS_FIX)
   - Criteria checklist with notes
   - Blocking issues list (if any)
   - Non-blocking recommendations
   - Confirmation of 5-plane proof (code, workspace, DB, env/compose, serving mode)

---

## 4. Verdict rules

- **PASS** — all criteria A–H satisfied, no blocking issues
- **BLOCKED** — any criterion in A–H missing or any plane of proof unverified
- **NEEDS_FIX** — minor issues, specify exactly what must change

If BLOCKED or NEEDS_FIX, do NOT mark READY_FOR_MERGE. Return to Agent 2 with specific fixes.

---

## 5. Merge gate

Even if PASS, do NOT merge without explicit user approval per AGENTS.md §7.

---

*REVIEWER_PROMPT.md v1 — generated by Agent 1 / Planner*
