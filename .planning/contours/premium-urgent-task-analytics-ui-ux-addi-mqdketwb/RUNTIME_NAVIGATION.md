# RUNTIME_NAVIGATION — Analytics UI/UX + Additional Fields + Excel Export

**Contour:** `premium-urgent-task-analytics-ui-ux-addi-mqdketwb`  
**Test environment:** `clearvestnic.ru:5177`  
**Local dev:** `http://localhost:5173`

---

## 1. Entry points

The analytics dashboards are reachable through the ProcessMap overlay:

| Surface | Typical route / trigger | Test ID |
|---------|------------------------|---------|
| Session Analytics | Open a session → Analytics tab | `session-analytics-dashboard` |
| Project Analytics | Open a project → Analytics tab | `project-analytics-dashboard` |
| Workspace Analytics | Open workspace → Analytics tab | `workspace-analytics-dashboard` |
| Analytics Hub | `/analytics` or overlay analytics entry | `analytics-hub-page` |

Note: exact URL patterns depend on `useAnalyticsRouteState.js` and the hosting overlay. Do not modify route builders `Mz()` or `TG()`.

---

## 2. Test URLs

```text
https://clearvestnic.ru:5177/
```

Use the application UI to navigate to:
1. Workspace Analytics
2. Project Analytics
3. Session Analytics

If direct routes are known, append them (e.g., `/analytics?scope=workspace`).

---

## 3. Verification flow

1. Open `https://clearvestnic.ru:5177/`
2. Log in if required
3. Navigate to Analytics
4. Select each scope (workspace, project, session)
5. Verify new columns/fields visible
6. Apply filters, verify results
7. Click "Export to Excel"
8. Verify downloaded file name and contents

---

## 4. Health checks

```bash
curl -s https://clearvestnic.ru:5177/health
curl -s -k https://clearvestnic.ru:5177/
```

---

## 5. Browser dev hints

- Look for `data-testid="analytics-export-excel"` for export button
- Look for `data-testid="analytics-data-table"` for table
- Look for `data-testid="analytics-cell-{key}"` for cells
- Filter chips: `data-testid="analytics-filter-chip"` (or class `.analyticsFilterChip`)
- Preset dropdown: `data-testid="analytics-preset-select"`

---

*RUNTIME_NAVIGATION.md v1 — generated by Agent 1 / Planner*
