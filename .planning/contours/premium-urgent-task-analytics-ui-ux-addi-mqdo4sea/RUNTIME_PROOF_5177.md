# Runtime Proof — clearvestnic.ru:5177

**Contour:** `premium-urgent-task-analytics-ui-ux-addi-mqdo4sea`  
**Deployed at:** 2026-06-14T11:15:45Z (per container label / build badge)  
**Git commit:** `4e8c0939`

---

## 1. curl smoke test

```bash
$ curl -s -o /dev/null -w "%{http_code}\n" http://clearvestnic.ru:5177/
200
```

Response headers (first request after deploy):

```
HTTP/1.1 200 OK
Server: nginx/1.27.5
Date: Sun, 14 Jun 2026 11:16:46 GMT
Content-Type: text/html
Content-Length: 439
Last-Modified: Sun, 14 Jun 2026 11:16:22 GMT
Cache-Control: no-cache, no-store, must-revalidate
```

Note: `https://clearvestnic.ru:5177/` returns an SSL handshake error in this environment; the served endpoint is `http://clearvestnic.ru:5177/`.

---

## 2. Deploy verification

```bash
$ ./verify-deploy.sh
Server: http://clearvestnic.ru:5177
Local git HEAD:  4e8c0939
Server /version: 4e8c0939
MATCH: local 4e8c0939 == server 4e8c0939
```

---

## 3. Screenshots

Captured via Playwright after logging in with the seeded admin account and selecting the **Default** org.

| Dashboard | Screenshot path |
|-----------|-----------------|
| Workspace analytics | `.local/processmap/screenshots/workspace-analytics-dashboard.png` |
| Project analytics | `.local/processmap/screenshots/project-analytics-dashboard.png` |
| Session analytics | `.local/processmap/screenshots/session-analytics-dashboard.png` |

Observed new columns:

- **Workspace:** Member Count, Project Count, Storage Used — all filterable, with an "Export to Excel" CTA.
- **Project:** Total Sessions, Last Activity, Owner — all filterable, with an "Export to Excel" CTA.
- **Session:** Duration, Status, Created By — all filterable, with an "Export to Excel" CTA.

---

## 4. Excel export proof

File generated by clicking **Export to Excel** on the Project dashboard:

```
.local/processmap/screenshots/processmap-analytics-project-2026-06-14.xlsx
```

Workbook contents verified with SheetJS/xlsx:

```
Sheets: [ 'Metadata', 'Data' ]
Metadata:
  [ [ 'Key', 'Value' ],
    [ 'surface', 'project' ],
    [ 'generatedAt', '2026-06-14T11:24:18.415Z' ],
    [ 'rowCount', 1 ],
    [ 'columns',
      'Сессия, Длительность, Действий, Крит. вопросы, Total Sessions, Last Activity, Owner' ],
    [ 'filters', '{}' ] ]
Data header:
  [ 'Сессия', 'Длительность', 'Действий', 'Крит. вопросы', 'Total Sessions', 'Last Activity', 'Owner' ]
```

---

## 5. Verdict

- Deploy succeeded.
- Server `/version` matches intended git commit.
- All three analytics dashboards render the new columns and filter inputs.
- Excel export downloads a workbook containing both `Metadata` and `Data` sheets, with visible columns, row count, and filters recorded in metadata.
- Runtime proof is valid for `http://clearvestnic.ru:5177/`; HTTPS on port 5177 is not terminating SSL in this environment.
