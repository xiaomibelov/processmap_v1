# VERSION_UPDATE_LEDGER_PROOF

Статус: `DONE`

Файл:

```text
/opt/processmap-properties-registry-part1/frontend/src/config/appVersion.js
```

Изменение:

```text
currentVersion: v1.0.126 -> v1.0.127
```

Changelog entry:

- `Реестр свойств добавлен в Аналитику как безопасная foundation-страница без fake-данных.`
- `Аналитика теперь открывает Реестр действий, Реестр свойств и будущие Дашборды из одного раздела.`
