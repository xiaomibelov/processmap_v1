# REGRESSION — analysis-tabs-ux-polish-p3

## Принцип

Всё функциональное, восстановленное в итерациях 1–2, остаётся на месте. Визуальные правки не убирают поведение.

| Элемент | Было | Стало | Функционал затронут? | Обоснование |
|---------|------|-------|----------------------|-------------|
| KPI-карточки (7 шт.) | 4 карточки, сырые ключи для mainline/avgStep/bpmnBinding | 7 карточек, переведённые подписи | нет | только подписи |
| Пустое состояние «Топ-3 ожидания» | 3 дублирующиеся строки | одно `AnalysisEmptyState` | нет | только подача |
| Статусы границ | «не заполнено» / «ok» / «не выбрано» | «Не выбрано» / «Заполнено» / «Триггер заполнен» | нет | только копи и логика статуса START |
| Логика START | требовался и trigger, и start_shop | достаточно trigger или start_shop | нет | статус теперь соответствует данным |
| AI-ошибки | сырой код `AI_PROVIDER_NOT_CONFIGURED` | человекочитаемое сообщение + технический код | нет | только копи |
| AI empty-state | показывался одновременно с error-state | показывается ровно одно состояние | нет | только UX |
| RAG-статус | русские строки захардкожены | i18n-ключи | нет | только копи |
| Тулбар «Действий» | без изменений | без изменений | нет | вне scope минимального патча |
| Группировка веток | без изменений | без изменений | нет | вне scope минимального патча |
| ProductActionsPanel / RagSearchPanel / PROCESSMAN | без изменений | без изменений | нет | не затронуты |

## Что не вошло в этот PR (оставлено на следующие итерации)

- Полная переработка тулбара сабтаба «Действия» (объединение «Привязки/Ещё/Дополнительно»).
- Группировка 43 переходов в «Ветках» по gateway.
- Тултипы на все icon-only кнопки строк таблиц.
- Индикаторы готовности на табах (dot/chip).
- Полная доступность (focus-ring, 44×44 targets) — частично покрыта через тултипы и семантику.
