---
atlas_fallback: true
contour: backend/notes-extraction-apply-boundary-v1
source_branch: backend/notes-extraction-apply-boundary-v1
date: 2026-05-07
obsidian_status: OBSIDIAN_WRITE_BLOCKED
---

# 22_AI слой и модули

## 2026-05-07 — notes extraction apply boundary

| Поле | Значение |
| --- | --- |
| Contour | `backend/notes-extraction-apply-boundary-v1` |
| Module | `ai.process.extract_from_notes` |
| Endpoint | `POST /api/sessions/{session_id}/notes/extraction-apply` |
| Verdict | `NOTES_EXTRACTION_APPLY_BOUNDARY_READY` |

Apply boundary added for notes extraction. The endpoint accepts only user-selected candidates from preview and applies them under strict diagram CAS.

Hard boundary:

- no DeepSeek/provider call;
- no fallback parser call;
- legacy `POST /api/sessions/{sid}/notes` unchanged;
- no BPMN XML mutation;
- no `product_actions` or export changes.

Changed keys are derived from accepted fields only: `notes`, `roles`, `start_role`, `nodes`, `edges`, `questions`.

