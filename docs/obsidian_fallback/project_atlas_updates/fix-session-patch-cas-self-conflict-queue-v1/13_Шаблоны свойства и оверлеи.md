---
title: "13 Шаблоны свойства и оверлеи"
type: project-atlas-update
contour: fix/session-patch-cas-self-conflict-queue-v1
date: 2026-05-01
status: source-tested
---

## Template/overlay regression scenario

> [!summary] Evidence
> Template apply / overlay path используется как доказанный сценарий race: `bpmn_meta` PATCH поднимал server version, а delayed interview/hydration PATCH отправлялся со старым base.

| Evidence | До | После |
| -------- | -- | ----- |
| `PATCH /sessions` bpmn_meta | 200, server `78 -> 79` | through `sessionPatchCasCoordinator` |
| delayed interview/hydration PATCH | stale `base=78`, `409`, `changed_keys=["bpmn_meta"]` | queued after accepted meta PATCH, base `79` |
| Template rendering/overlay behavior | unchanged goal | unchanged |

> [!warning] Scope boundary
> Контур не меняет template apply rendering, overlays, 409 UX или durable BPMN XML truth.
