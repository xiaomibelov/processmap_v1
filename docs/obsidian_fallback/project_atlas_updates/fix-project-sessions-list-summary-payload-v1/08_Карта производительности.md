---
atlas_note: "08_Карта производительности"
contour: "fix/project-sessions-list-summary-payload-v1"
status: "runtime-stage-pending"
date: "2026-04-30"
source: "fallback"
---

# 08_Карта производительности

> [!warning]
> Obsidian MCP не нашёл `PROCESSMAP/PROJECT ATLAS/...`, поэтому запись подготовлена как fallback update в репозитории.

## fix/project-sessions-list-summary-payload-v1

> [!summary]
> Контур уменьшает payload списка сессий проекта через `view=summary` для `GET /api/projects/{id}/sessions`. Старый full contract сохранён через отсутствие `view` или `view=full`.

| Метрика | До | После | Комментарий |
| ------- | -- | ----- | ----------- |
| Endpoint | `GET /api/projects/{id}/sessions` | `GET /api/projects/{id}/sessions?view=summary` | Frontend bootstrap использует summary mode |
| Stage payload | `4,803,702 bytes` | `RUNTIME_STAGE_PROOF_PENDING` | Deploy не выполнялся |
| Stage duration | `4448ms` | `RUNTIME_STAGE_PROOF_PENDING` | Нужен stage после deploy |
| Local synthetic payload | `4,105,730 bytes` | `5,704 bytes` | 12 heavy sessions |
| Local synthetic duration | `236.16ms` | `12.11ms` | Direct backend function proof |
| Снижение JSON объёма | baseline | `~720x` | На синтетическом наборе |

> [!success]
> Summary response не содержит `bpmn_xml`, полный `interview`, `bpmn_meta`, `notes`, `notes_by_element`, `analytics`, `resources`, `normalized`, `publish_git_mirror`.

Связанные заметки: [[06_Backend API карта]], [[14_Журнал runtime evidence]], [[15_Backlog контуров]].
