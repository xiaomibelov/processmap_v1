# ADDENDUM-4 к UXF: хедеры — версии, точки входа, пометки in progress — отчёт

Дата: 2026-07-31 · Ветка: `feature/uxf-tobe-workflow-ux` · Приёмка: `scripts/uxf_check_addendum4.mjs` → **EXIT=0**

## B1. Блок сохранения/версионирования — единый паттерн «иконка+счётчик»
- Слева в среднем хедере (наследие A7) теперь две пары-капсулы единого стиля
  (одна высота 32px, одинаковая типографика/отступы, обводка по токенам):
  - **[💾 Сохранить · Rev. N]** — Rev = ревизия сохранённого состояния
    (`diagram-toolbar-diagram-state-version-chip`), привязана к сохранению;
    тултип пары: «Сохранить · ревизия N»; у иконки — свой детальный тултип,
    у чипа — расшифровка (включая конфликт версий, `text-danger` + pulse).
  - **[⑂ Версия · V. N]** — V = номер версии BPMN (`diagram-toolbar-version-chip`),
    привязана к созданию версии; тултип пары: «Новая версия · текущая V.N».
- Порядок: [Сохранить Rev] → [Версия V] → (no-diff hint) → далее группа
  представлений в центре. Порядок testid'ов (save < createRevision < versionChip)
  сохранён — контрактный тест 5/5 зелёный без изменений.
- Скрин крупным планом: `addendum4_b1_pairs_closeup.png`.

## B2. «Создать TO BE» убрана из среднего хедера
- Кнопка `diagram-toolbar-tobe-entry` **удалена** (дубль и визуальный шум).
- Точки входа остались и проверены приёмкой:
  1. переключатель «Схема | TO BE» в группе представлений (вход без перезагрузки);
  2. секция TO BE в сайдбаре («Из этого процесса» → «Открыть TO BE», скрин
     `addendum4_b2_sidebar_entry.png`).
- **Стрелки ↶/↷ и «⋯» — статус зафиксирован: оставлены.** Это реальные
  действия (undo/redo активны на вкладке Diagram с disabled-состояниями и
  тултипами «Шаг назад»/«Повторить отменённое действие»; «⋯» — рабочее меню
  импорта/шаблонов). Стиль единый: `secondaryBtn h-8 w-8` иконки.
- Регрессионные скрипты addendum-1/block2 адаптированы под B2 (вход в TO BE
  теперь через сегмент — как и задумано).

## B3. Пометки «in progress»
- Бейдж «in progress» — внутри кнопки, **слева сверху** (absolute в углу,
  не отдельная строка, выравнивание группы не ломается):
  - «Анализ процессов», «TO BE» (в сегменте — и в хедере, и в панели TO BE),
    «DOC», «DOD» — **с пометкой**;
  - «Diagram (BPMN)», «Схема», «XML», «Аналитика» — **без пометки**.
- Тултип бейджа и кнопки: «Представление в разработке — часть функций может
  отсутствовать». (Нативные title-тултипы в headless-скриншоты не попадают —
  верифицировано по атрибутам; hover записан в скринкасте
  `addendum4_b3_in_progress_hover.webm`.)
- Размер подобран замером: 6px lowercase — 46px, помещается даже в узкие
  кнопки DOC/DOD (53–54px); верифицировано rect-ами (бейдж строго внутри
  кнопки, слева сверху).
- **Пустых вкладок нет** (проверено кликами): DOC = «Документ процесса»
  (сводка, варианты R0/R1/R2), DOD = «Готовность PM» (52%, блокеры),
  «Анализ процессов» = интервью — контент реальный, кандидатов на скрытие
  нет. Скрины: `addendum4_tabreal_{interview,doc,dod}.png`.
- Пометка информативна, вкладки открываются как раньше (не блокирует).

## Приёмка (EXIT=0)
1. Пары — единый паттерн, 32px, тултипы «Сохранить · ревизия 1» /
   «Новая версия · текущая V.—», порядок верный.
2. `diagram-toolbar-tobe-entry` отсутствует; оба входа (сегмент, сайдбар) живы.
3. 4 бейджа внутри кнопок слева сверху с тултипами; Diagram/Схема/XML/Аналитика чистые.
4. undo/redo/⋯ — оставлены (реальные действия), тултипы на месте.
5. Регрессия: вход/возврат TO BE без перезагрузки (`navCount===0` обе стороны),
   рабочее место и сайдбар не сломаны.

## Скрины
- `addendum4_b1_pairs_closeup.png` — пары крупным планом.
- `addendum4_b3_tabs_in_progress.png` — группа представлений с бейджами.
- `addendum4_b2_right_actions.png` — правый слот без «Создать TO BE».
- `addendum4_b2_sidebar_entry.png`, `addendum4_tobe_mode.png`,
  `addendum4_tabreal_*.png`.
- «До» — `addendum3_segment_after_diagram_tab.png`.

## Регрессия (всё EXIT=0, preview 127.0.0.1:5198 → stage API)
- `uxf_check_addendum.mjs` (A1–A4, адаптирован под B2) — зелёный.
- `uxf_check_addendum2.mjs` (A5–A9) — зелёный.
- `uxf_check_addendum3.mjs` (хедеры) — зелёный.
- `uxf_check_block2.mjs` (адаптирован: вход через сегмент) — зелёный.
- `w4_tobe_current_fix_check.mjs` (#627) — зелёный.
- `node --test src/features/process` — baseline; контрактные тесты хедера 5/5.

## Затронутые файлы
- `frontend/src/features/process/stage/ui/ProcessStageHeader.jsx` — B1 (пары),
  B2 (удаление кнопки), B3 (бейджи вкладок).
- `frontend/src/components/ModeSwitchSegment.jsx` — B3 (бейдж «TO BE»).
- `scripts/uxf_check_addendum4.mjs` — NEW; адаптация `uxf_check_addendum.mjs`,
  `uxf_check_block2.mjs` под B2.

## Не тронуто
bpmn.io канвас, XML E7, Validation E6, RBAC, i18n, overlay OL1, read-only
AS IS md5-инвариант, логика undo/redo и overflow-меню, контракт testid'ов.
