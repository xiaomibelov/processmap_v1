# pm-20260611-003: Wire frontend lightweight overlays

## Scope
FRONTEND

## Status
IN_PROGRESS (2026-06-10T20:16Z)

## Description
Integrate the new lightweight `/api/sessions/{sid}/overlays` JSON endpoint into the React frontend. Replace monolithic 216 KB XML overlay loading with 872-byte JSON split-loading. Add feature flag `lightweightOverlays` to allow safe fallback.

## Inputs
- backend/app/overlay_cache.py — `get_overlays_json()` returns `list[dict]` with keys: id, node_id, x, y, width, height, text, style, meta
- frontend/src/lib/api.js — `apiGetOverlays(sid)` already exported
- frontend/src/lib/apiRoutes.js — `overlays: (sid) => /api/sessions/${sid}/overlays` already defined
- frontend/src/lib/bpmnApi.js — exports `apiGetOverlays`

## Risks
| Risk | Level | Mitigation |
|---|---|---|
| AI overlay regression | HIGH | Pixel-perfect match verification against legacy path |
| Tab navigation breakage | MEDIUM | e2e tab-transition-matrix-big.spec.mjs must pass |
| Feature flag default | LOW | Default false until verified |

## DoD
□ Feature flag `lightweightOverlays` in `window.__FPC_CONFIG__`
□ `BpmnStage.jsx` calls `apiGetOverlays` after base diagram load
□ `mountOverlays()` renders `foreignObject` layer with exact x/y/w/h from JSON
□ Pixel-perfect match with existing overlay positions
□ Fallback to legacy monolithic XML path if `flag=false`
□ No regression in tab navigation or AI panels
□ e2e `tab-transition-matrix-big.spec.mjs` passes
□ e2e `bpmn-roundtrip-big.spec.mjs` passes
□ Kanban updated: task moved to DONE with exhaustive Files list

## Files (modified)
- [x] frontend/src/components/process/BpmnStage.jsx — added clearLightweightOverlays, mountLightweightOverlays, integrated into renderViewer/renderModeler/destroyRuntime
- [x] frontend/src/features/process/hooks/useBpmnSync.js — pass includeOverlay=false when __FPC_LIGHTWEIGHT_OVERLAYS__ is true

## Verification pending
- [ ] Build passes (npm run build)
- [ ] e2e tab-transition-matrix-big.spec.mjs
- [ ] e2e bpmn-roundtrip-big.spec.mjs
- [ ] Browser pixel-perfect match test
