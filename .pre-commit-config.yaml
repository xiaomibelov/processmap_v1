repos:
  - repo: local
    hooks:
      - id: scan-secrets
        name: Scan for potential secret leaks
        entry: python tools/security/scan-secrets.py
        language: system
        pass_filenames: true
        stages: [pre-commit]
